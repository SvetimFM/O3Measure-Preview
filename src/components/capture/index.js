/**
 * Capture System Components
 * AR photo capture and documentation tools
 */

// Import capture components
import './photo-capture.js';

console.log('[Capture System] Photo capture components loaded');

export default {
  loaded: true
};
